{
  "name": "igt-nexgen-codes",
  "version": "5.4.0",
  "description": "A mapping of common codes used by M5 to friendly names.",
  "script": {},
  "devDependencies": {
    "grunt": "0.4.5",
    "grunt-env": "0.4.4",
    "grunt-jsdoc": "2.3.0",
    "grunt-mocha-istanbul": "2.4.0",
    "grunt-mocha-test": "0.12.7",
    "istanbul": "0.3.22",
    "mocha": "2.5.3"
  },
  "scripts": {
    "deploy": "mkdir -p ../bin/codes && cp -a lib ../bin/codes/ && cp -a package.json ../bin/codes/"
  },
  "private": true
}
